/**
 * src/types/content.ts — frontend-standards 규약상 표준 타입 진입점.
 *
 * 실제 타입 정의의 단일 진실원은 src/lib/types.ts (data-architect 소유, 01_schema.md 계약).
 * 이 파일은 표준 import 경로(@/types/content)를 위한 re-export 쉼이다.
 * 컴포넌트는 `@/types/content` 에서 타입을 가져온다.
 */
export * from "@/lib/types";
